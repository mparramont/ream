<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :input_data %>
    <%= form.text_field :input_data %>
  </div>

  <div class="field">
    <%= form.label :output_data %>
    <%= form.text_field :output_data %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
